'use client';

import { INSPIRATION_IMAGES } from '@/lib/data/inspiration';
// Import any other hooks or client components you were using (e.g. useState, useEffect)

export default function HomeContent() {
  return (
    <main>
      {/* 
        Paste all the JSX code you previously had inside your Page component here.
        All your interactive state, onClick handlers, and client hooks go here.
      */}
      <h1>Haosail | Bespoke Wallpaper & Murals</h1>
    </main>
  );
}
